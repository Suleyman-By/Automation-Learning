const name = "Solo";
const country = "Vietnam";
console.log(`${name} lives in ${country}`);

const product = "Laptop";
const price = 1500;

console.log(`${product} costs $${price}`);

const a = 15;
const b = 25;

console.log(`${a} + ${b} = ${a + b}`);

const user = {
  name1: "Solo",
  age: 25,
};
const { name1, age } = user;
console.log(`${name1} is ${age} years old.`);

const city = "Hanoi";
const country1 = "Vietnam";

console.log(`${city} is in ${country1}`);
